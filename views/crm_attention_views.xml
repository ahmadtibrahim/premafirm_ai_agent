<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Needs Attention drill-down list (client action + CRM menu) -->
    <record id="crm_needs_attention_list_action" model="ir.actions.client">
        <field name="name">Needs Attention</field>
        <field name="tag">premafirm.crm_attention_list</field>
    </record>

    <menuitem id="menu_premafirm_crm_needs_attention"
              name="Needs Attention"
              parent="crm.crm_menu_root"
              sequence="5"
              action="crm_needs_attention_list_action"/>
</odoo>
